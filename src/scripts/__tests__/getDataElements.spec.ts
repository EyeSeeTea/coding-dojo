import { getDataElements } from "../get-data-elemets";

describe("getDataElements", () => {
    it("should return an array of data elements", () => {});

    it("should throw NonAdminError if user is not admin", () => {});
});
